<x-layouts.public :title="'Careers'"> @php($lang = request()->route('lang', 'en'))     <x-hero :title="$lang === 'de' ? 'Karriere' : 'Careers'" :subtitle="$lang === 'de' ? 'Offene Stellen bei HOPn.' : 'Open positions at HOPn.'" />     <section class="container-shell mt-8 space-y-4">         @forelse($jobs as $job)             <div class="card-panel p-5 flex items-center justify-between">                 <div>                     <h3 class="text-lg font-semibold text-white">{{ $job->title }}</h3>                     <p class="text-sm text-slate-400">{{ $job->location }} · {{ ucfirst($job->type) }}</p>                 </div>                 <a href="{{ route('careers.show', ['lang' => $lang, 'slug' => $job->slug]) }}" class="btn-primary text-sm">{{ $lang === 'de' ? 'Mehr erfahren' : 'Learn More' }}</a>             </div>         @empty             <p class="text-center text-slate-400">{{ $lang === 'de' ? 'Derzeit keine offenen Stellen.' : 'No open positions at this time.' }}</p>         @endforelse     </section>     <section class="container-shell">         <x-pagination :paginator="$jobs" />     </section> </x-layouts.public>