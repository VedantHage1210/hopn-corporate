<x-layouts.public :title="'Products'"> @php($lang = request()->route('lang', 'en'))     <x-hero :title="$lang === 'de' ? 'Produkte' : 'Products'" :subtitle="$lang === 'de' ? 'Digitale Produkte für geschäftliche Wirkung.' : 'Digital products and accelerators for business impact.'" />     <section class="container-shell mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">         @foreach($products as $product)             <article class="card-panel p-5">                 <h3 class="text-lg font-semibold text-white">{{ $lang === 'de' && $product->title_de ? $product->title_de : $product->title_en }}</h3>                 <p class="mt-2 text-sm text-slate-300">{{ $lang === 'de' && $product->summary_de ? $product->summary_de : $product->summary_en }}</p>                 <a href="{{ route('products.show', ['lang' => $lang, 'slug' => $product->slug]) }}" class="mt-4 inline-block text-sm text-indigo-300">{{ $lang === 'de' ? 'Produkt ansehen' : 'View Product' }}</a>             </article>         @endforeach     </section>     <section class="container-shell">         <x-pagination :paginator="$products" />     </section> </x-layouts.public>