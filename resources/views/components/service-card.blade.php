@props(['service']) @php($lang = request()->route('lang', 'en')) <article class="card-panel p-5">     <h3 class="text-lg font-semibold text-white">{{ $lang === 'de' && $service->name_de ? $service->name_de : $service->name }}</h3>     <p class="mt-2 text-sm text-slate-300">{{ $lang === 'de' && $service->summary_de ? $service->summary_de : $service->summary }}</p>     @if(!empty($service->slug))         <a href="{{ route('services.show', ['lang' => $lang, 'slug' => $service->slug]) }}" class="mt-4 inline-block text-sm text-indigo-300 hover:text-indigo-200">{{ $lang === 'de' ? 'Mehr lesen' : 'Read more' }}</a>     @endif </article>