@props(['testimonial']) @php($lang = request()->route('lang', 'en')) <blockquote class="card-panel p-5">     <p class="text-slate-300 italic">"{{ $lang === 'de' && $testimonial->quote_de ? $testimonial->quote_de : $testimonial->quote_en }}"</p>     <footer class="mt-3">         <span class="text-sm font-semibold text-white">{{ $testimonial->author_name }}</span>         <span class="text-xs text-slate-400"> {{ $testimonial->author_role }} @ {{ $testimonial->company }}</span>     </footer> </blockquote>